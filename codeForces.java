import java.util.*;

public class codeForces {
    public void Prime_Deletion () {
        Scanner sc = new Scanner(System.in);
        int t = Integer.parseInt(sc.nextLine());
        for (int i = 0; i < t; i++) {
            String s = sc.nextLine();
            for (int j = 0; j < 9; j++) {
                if (s.charAt(j) == '1') {}
                if (s.charAt(j) == '7') {}
            }
        }
    }
}
